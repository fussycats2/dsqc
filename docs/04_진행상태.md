# 진행 상태 (저장 시점: 2026-05-29)

다음 세션에서 이어서 작업하기 위한 핸드오프 메모.

## 한 줄 요약
엑셀 `업로드.xlsm`(귀금속 제조공정 중량/로스/입출고 시스템)을 Next.js+Supabase+Vercel(무료)로 이전 중.
골격·DB·작성 입력·탭 네비·다크모드까지는 동작. **공정 화면의 핵심 액션 로직(완료/집계/투입/이관/분할)은 아직 원본과 불일치 — 다음에 하나씩 재검증 필요.**

## 실행/환경
- 앱: `web/` (Next.js 16, App Router). `cd web && npm run dev` → http://localhost:3000
- `web/.env.local` 에 Supabase URL + publishable(anon) key 설정됨(사용자 로컬).
- Supabase 클라우드 무료 프로젝트. SQL 적용 순서:
  1. `supabase/migrations/0001_init.sql`
  2. `supabase/seed.sql`
  3. `supabase/migrations/0002_locked.sql`  ← lots.locked 컬럼 (필수)
  4. 임시 RLS 허용(로그인 전 확인용): `*_anon_all` 정책 (이미 적용됨, 로그인 슬라이스에서 잠글 것)

## 지금까지 만든 것 (동작 확인됨)
- 공정 마스터 40개 시드, 18K/14K·io/work/entry·검수 구분
- **작성**: 다중행 입력 → 대상 공정 일괄 보내기(입고), Tag검증(1tag=0.035g), 일련번호 최초 생성
- **탭 네비**: 18K/14K → 부서/공정/검수 → (공정: 연마/빠우/뻥) 계단식 필터 + 작성/대시보드 고정탭
- **대시보드**: v_process_balance로 파트별 입고/출고/재고
- **다크모드 토글**(밝게/어둡게/시스템), **작업일 토글**, 중량 소수 2자리(셋째자리 입력 차단)

## ★ 미해결: 공정 액션 로직 (다음 세션 최우선)
사용자 피드백 — "로직이 다 잘못되어 있다, 하나씩 잡아야 함".
원본 VBA와 1:1 재대조하며 검증할 대상:
- **완료 처리/집계** (Module7 集計): 작업중→완료 전이. 일련번호 유지/그룹화, 작업전=입중량, 작업후/실중량 수동.
  → 현재 구현(`completeLots`)이 실제 워크플로우(어느 버튼/순서/대상열)와 맞는지 재확인.
- **투입**(Module4, 작업중 A:K→대상) vs **이관**(Module9, 완료 M:Z→대상 B:K) 컬럼 매핑 정확성.
- **출고**(Module10 출고_선택행_복사_공용), **타부서투입**(Module16) 아직 미구현.
- 분할(Module1/11 SplitNumberByCount), 삭제/위로올림(Module6/8/23/24/32) 재확인.
- io 계열(기계/검수)의 입고→출고 전이가 work와 어떻게 다른지(실중량/Tag수정/Tag중량/Tag로스/출고중량) 원본 확인.

### 재검증 방법(권장)
- `_vba/`에 디컴파일된 86개 모듈 원본 있음. 기능별로 한 개씩:
  1. 해당 VBA Sub의 입력(선택 범위)·출력(대상 열)·부수효과(색칠/잠금/로그) 정독
  2. 사용자에게 "이 동작이 맞냐" 확인
  3. 그 다음에 web 액션(`web/src/app/process/[id]/actions.ts`) 수정
- 즉, 코드 먼저 짜지 말고 **VBA 정독 → 사용자 확인 → 구현** 순서.

## 코드 위치
- 액션: `web/src/app/process/[id]/actions.ts` (sendRows/completeLots/transferInbound/transferOutbound/splitLot/updateLot/deleteLots)
- 공정 화면: `web/src/app/process/[id]/ProcessView.tsx`, `page.tsx`, 작성=`EntryGrid.tsx`
- 컬럼정의/계산: `web/src/lib/types.ts` (COLUMNS, shipWeight/lossOf/lossRateOf)
- 일련번호 그룹: `web/src/lib/serial.ts`
- 탭/날짜/테마: `web/src/components/{TabBar,DateToggle,ThemeToggle,NumberInput}.tsx`
- 스키마: `supabase/migrations/0001_init.sql`, `0002_locked.sql`, `supabase/seed.sql`
- 분석/계획: `docs/01_분석.md`, `docs/02_리팩토링계획.md`, `docs/03_셋업가이드.md`

## 확정된 도메인 규칙(불변)
- 일련번호: 최초 1회 생성(작성→보내기, 대상 약어 M_/QM_…), 이동 시 **유지**, 병합/분할 때만 형태 변경.
- 처리·이동된 원본 행은 **잠금**(재작업 불가).
- 작업후/실중량/Tag류는 **수동 입력**, 로스=작업전−작업후·출고중량=실중량+Tag−Tag중량 자동.
- 공정 2계열: io(일반+검수) / work(연마·뻥·빠우).
- 마감=일/월, 미작업 재고 이월. 품질결산서=LAN 공유 .xlsm 로컬 브릿지. 데스크톱 전용. 단일 공용계정.

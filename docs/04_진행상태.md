# 진행 상태 (갱신: 2026-06-11)

## ✅ 2026-06-11 세션(2차) — 성능 개선: 탭 전환·액션 왕복 단축 (v1.3.4)
빌드 green(tsc·eslint·vitest 37·next build). 사용자가 커밋/배포 직접 수행. **기존 로직 무영향 원칙**(사용자 지시) 하에 진행. 범위: 탭 전환 + 자주 쓰는 공정 액션만 — 결산서·날짜전환·마감·백업·가져오기는 제외(사용자 지정).

### 적용 내역
- **loading.tsx 2개 신설**(`app/loading.tsx` · `app/process/[id]/loading.tsx`): 동적 페이지 네비게이션이 서버 렌더 완료를 기다리지 않고 즉시 커밋 → 본문만 스켈레톤(헤더·하단탭 유지). 체감 개선 핵심. 순수 표시용 — 로직 없음.
- **그룹 일괄 prefetch**: TabBar 분류 드롭다운·NavContextMenu 서브메뉴가 **열리는 순간** `warmGroup()`으로 그 그룹 공정 전체 prefetch — 항목 고르는 사이에 서버 렌더 완료(기존 항목 호버 warm은 호버→클릭 간격이 짧아 lead time 부족했음).
- **fetchAll PAGE 1000→2000**: 하루치 전량 조회(대시보드 등) 2~3왕복→1왕복. ★Supabase Max Rows(현 5000)를 2000 미만으로 낮추면 조용한 데이터 누락 — `fetchAll.ts` 주석에 명기.
- **actions.ts 공정 마스터 캐시화**: `nameOf`/`schemaOf`/`tagConfirm` 검수목록이 매번 DB 조회하던 것 → `getProcesses()` 캐시(요청 내 dedupe+TTL 5분). 이동류·집계의 선행 왕복 제거(공정 이름/타입은 런타임 불변 전제).
- **`insertLotsLinked` 헬퍼 + 0016 RPC**(`insert_lots_linked(p_lots,p_links)` 단일 트랜잭션): 이동류(투입/이관/출고/타부서)·집계·분할의 "lots insert→lot_links insert" 2콜을 1콜 원자 처리 — 중간 실패 시 전체 롤백이라 '고아 행 수동 회수' 경로 소멸(선점유 해제는 종전대로 호출부). **RPC 미적용이면 PGRST202 감지 후 기존 2콜+회수 방식으로 자동 폴백**(0012 next_serials 패턴). 도메인 매핑(VBA 1:1 계산·buildGroupedSerial)은 JS에 유지 — SQL 복제 안 함(로직 무영향 원칙). 이동류·집계 4왕복→2왕복, 분할 4→3.
- **인프라(사용자 수행·확인 완료)**: 0016 SQL편집기 적용 완료. **Vercel 함수 리전 워싱턴(iad1)→서울(icn1) 변경**(Supabase는 원래 Northeast Asia) — 배포 후 x-vercel-id로 icn1 확인. 변경 전엔 DB 왕복마다 ~180ms 추가되던 상태 → 액션당 0.4~1초 단축.
- changelog **v1.3.4 "속도 개선"** 추가.
- **지연 스피너**(`LoadingOverlay.tsx`, 후속 피드백): pending 180ms 이후에만 표시 — 빠른 전환(프리페치 적중·스켈레톤 즉시 커밋)에서 "스피너 번쩍→스켈레톤" 이중 깜빡임 제거. 느린 전환·서버 액션(작업일 변경 등)에서는 종전대로 보임. ※ ESLint `react-hooks/set-state-in-effect` 때문에 setState는 타이머 콜백/cleanup에서만 호출.
- **스켈레톤 실제 골격화**(후속 피드백): 단순 회색 막대 → 실제 화면 구조 모사(공정=제목 줄·툴바 알약·표 카드 2장(2xl 좌우)·머리띠+칸 단위 행, 대시보드=일마감 박스·섹션 라벨·카드 2+4장), 행은 아래로 갈수록 페이드.

### 미적용 결정
- `experimental.staleTimes`(dynamic 30초 라우터 캐시): 다기기(공용계정) 데이터 신선도 트레이드오프로 **사용자 거부** — 재제안 금지.

---

## ✅ 2026-06-11 세션 — 우클릭 네비 · 호버 펼침 통일 · 나누기/계보 개편 · 결산서 화면 (v1.3.0~v1.3.3)
빌드 green(tsc·eslint·vitest 37·next build). 사용자가 버전 분리·커밋 직접 수행.

### v1.3.3 — 결산서 입력 화면 개편 (인쇄 불변)
- **화면만 변경, 전부 `print:` 변형으로 인쇄 시 원상 복구** — 인쇄 결과물은 기존 엑셀 양식 그대로(상하 배치·결재란·크기). (`SettlementView.tsx`만 수정, JSX/클래스 계층)
- 화면: K18·K14 **좌우 배치**(flex-wrap — 좁으면 자동 상하), 결재란 `hidden print:flex`, 글자 10→12px·헤더 9→11px·열너비 60/56/50→72/66/58px 확대.
- 로직 무변: 상태(vals)·셀 주소·derive 계산·자동저장·결산전송/백업/가져오기/마감이월/날짜변경(`settlementActions.ts`, `/api/settlement/*`) 전부 미수정. 붙여넣기=주소 기반, 방향키=좌표 기반이라 배치 변경과 무관.
- **로그인 화면 새단장**(`login/page.tsx`, 시각만 — 인증 로직 무변): 금빛 그라데이션 배경+블롭, 반투명 블러 카드+골드 라인+등장 애니메이션, 입력/버튼 확대. 제작자·빌드 정보는 좌하단 유리 카드 하나로 통합(우하단은 어두운 블롭 위라 밝은 카드가 떠 보여 제거 — 사용자 결정).

### v1.3.0 — 우클릭 네비게이션
- 본문 우클릭 → 커서 위치 이동 메뉴(`NavContextMenu`, Radix ContextMenu). 18K/14K 전환은 `KaratProvider`가 단일 출처(하단탭과 동기화), `MenuScrim`(z-25)으로 배경 어둡게+흐리게. 입력칸·텍스트 선택 위에서는 브라우저 기본 메뉴 유지.

### v1.3.1 — 대상 드롭다운 UX 통일
- **호버 펼침 로직 단일화**: `lib/useHoverMenu.ts` 공용 훅 신설 — 하단탭(TabBar)·공정 액션툴바(ProcessView `TargetAction`)·작성 전송바(EntryGrid 입고/출고)가 같은 훅 사용(150ms 유예 닫기, 터치는 기존 탭 동작). 메뉴 열린 동안 스크림 표시 + 버튼 바를 z-26으로 부양(하단탭과 동일 시각효과).
- **대상 목록 구분선**: `menuGroups.ts`에 `workGroupOf`(연마|빠우|뻥)·`ioGroupOf`(부서|검수) 추가 — 공정투입/공정이관·타부서출고·작성 입고/출고 메뉴에 적용.
- **토스트 시인성**: 진한 단색 배경(성공 emerald-600·오류 rose-600·경고 amber-600·info blue-600, 흰 글씨)+테두리 2px·그림자·420px 폭·글자 확대. 일반(toast.message)은 테마 반전 고대비. duration 3.2초 유지(사용자 결정). (`ui/sonner.tsx`, `Chrome.tsx`)

### v1.3.2 — 나누기·계보 추적 개편
- **나누기 데이터 모델 변경**(`splitLotCustom`): 원본 삭제 → **원본 행을 첫 조각(-1)으로 갱신** + 나머지 조각(-2~-n) 신규 생성 + 원본→조각 `split` lot_links. 원본이 남아 상류 링크(이동·이월)가 안 끊김 → 작성에서 바로 입고된(부모 링크 없는) 행도 계보 기록됨. 행수·수량/중량 합·일련번호 표기(-1~-n)는 종전과 동일(회계 무변), tag/q/원중량은 종전처럼 조각에 비움.
- **조각 위치 일관성**: 조각이 원본 `created_at` 승계 — 표 정렬(created_at→serial→id)의 2차 키 serial이 조각을 원본 자리에 나란히 모음(종전엔 새 조각만 맨 아래로).
- **계보 모달 관계도화**(`GenealogyModal`): 시간순 일렬 나열 → 기준 행 + '어디서 왔나/어디로 갔나' 들여쓰기 트리(집계=여러 출처 가지가 합쳐지는 모양). 카드 클릭으로 재루팅(타고 들어가기, 서버 재조회 없음)+뒤로 스택. **가상 '나누기 전 원본' 합성**(`synthesizeSplitOriginals`, 표시 전용): -1 일련번호 패턴일 때만 `001→(분할)→001-1~-n`으로 재배선, 수량/중량은 조각 합(재분할 재귀)으로 복원. DB 무변.
- 과거(원본 삭제 방식)에 나눈 데이터는 소급 안 됨(당시 링크 부재).

---

## ✅ 2026-06-10 세션 — 전체 점검(감사) + 안정성 수정 일괄 (v1.2.2)
정식 운영 중(v1.0.0 2026-06-02 배포 후 1.1.0/1.2.0/1.2.1 거침). 이번 세션 = 코드 전체 점검 후 발견 이슈 수정.

### 수정 완료 (빌드 green: tsc·eslint·vitest 37)
- **결산서 자동저장 경합 수정**: 저장 요청 비행 중 입력한 값이 '저장됨'으로 오표시되고 실제 미저장되던 버그 → 입력 세대 카운터(`editGen`)+단일 비행(`savingRef`)+최신 스냅샷(`stateRef`)으로 해결, 비행 중 입력 시 응답 직후 이어서 재저장. (`SettlementView.tsx`)
- **일반 삭제 동시작업 안전**: `deleteLots`도 선점유(claimUnlocked) 컨벤션 적용 — 다른 기기가 막 잠근(집계/이동) 행을 stale 화면에서 삭제하는 구멍 차단. 잠금행 삭제는 `includeLocked=true` 명시 경로만. (`actions.ts`)
- **계보(lot_links) 기록 실패 시 롤백**: 집계·이동·분할 3곳 — 생성행 회수+점유 원복 후 "계보 기록 실패(원복됨)" 에러. (`actions.ts`)
- **작성 붙여넣기**: 숫자 칸(수량·중량·Tag·Q) 콤마 제거(기존엔 "1,234.50"이 NaN→null로 조용히 유실). (`EntryGrid.tsx`)
- **그리드 복사(Ctrl+C)**: 콤마 제거를 숫자 칸(inputMode decimal/numeric)만으로 한정 — 내역·비고 텍스트의 콤마 보존. (`useGridSheet.ts`)
- **로그인 두 번 해야 되던 문제(근본 원인 발견)**: 컴퓨터 장시간 종료 후 인증 쿠키는 사라져도 `last_seen`(30일)이 옛 값으로 남음 → 로그인 성공 직후 첫 요청에서 proxy가 '5시간 무활동' 오인, 새 세션 삭제. 해결=로그인 성공 시 `last_seen`을 현재 시각으로 초기화. (`login/page.tsx`)
- **일마감 이월 계보 연결**: 이월 복사본에 id 직접 부여 + 원본→복사본 `carry`("이월") lot_links — 날짜 경계에서 계보 추적이 안 끊김. (`closeActions.ts`, `types.ts`)
- **일련번호 동시 발번 중복 차단**: max+1 방식의 'RPC 발번→insert' 틈 경합 → `serial_counters`(공정×일자) upsert 행잠금으로 원자 발번. 발번 후 insert 실패 시 번호 건너뜀(중복보다 안전). (`0014` 마이그레이션)
- **결산서**: 계산칸 0도 `0.00` 표시(빈칸=미계산과 구분) · K14 분석업체 행머리 "K18"→"K14" 오타 수정.
- **계보 모달 시각 KST 고정**(+9h) — 기기 시간대와 무관하게 표와 일치. (`ProcessView.tsx`)

### 점검 후 '그대로 둠' 확정(사용자 결정 — 재제안 금지)
- **원중량은 현재 아예 사용 안 함** → 집계 후 콤마결합 원중량이 산식(`N()`=0)에서 무시돼도 무방.
- **공정이관 대상에 자기 자신 포함 = 정상**(같은 공정 재작업 흐름).
- **부서·검수는 로스 개념 없음** → 스냅샷의 io lossW 음수값 무해(표시 안 함).
- **대시보드 날짜변경은 결산서를 같이 안 옮겨도 됨**(결산서 날짜변경 별도 사용).

### 배포 상태
- **0014 마이그레이션 적용 완료**(사용자 확인, cron jobid 2 = `purge-serial-counters` KST 04:05 정상).
- changelog v1.2.2 작성 완료 — 사용자가 커밋/배포 예정.

---

## ✅ 2026-05-31 세션 마무리 — 현재 상태
- **DB 마이그레이션 전부 적용 완료**(사용자 확인): `0001`→`seed`→`0002`→`0003`(엑셀 4열)→`0004`(빠우 순서). 추가 적용 필요 없음.
- **공정/부서/검수 화면 + 작성 = 기능·UI 완성도 높음, 빌드 green.** 핵심 흐름(보내기·투입·타부서투입·집계(작업완료)·이관·현장/검수출고·나누기·삭제·잠금해제·수정·Tag보정/확정·인쇄)·조합찾기·합계표시 동작.
- **사용자 로직 재검증 대기**(다음 세션): feedToWork 중량(K)=원본 중량+Tag+Q+원중량 합, shipToIo 실중량=작업후−Tag−Q−원중량, completeLots 작업전=중량(K)합 — 전부 VBA 그대로 옮김. 사용자가 실제 값으로 검증 예정.
- **남은 기능(다음 세션)**: ①일마감(매일+미작업 이월, 월마감X) ②품질결산서 로컬경로 저장(Module37 매핑 유지) ③로그인+RLS 잠금 ④홈 대시보드 모던화(맨 마지막, 단순 UI).
- **세부 UI 미세조정 보류**(단순 UI는 마지막): 칸 너비 등.

## 핵심 파일
- 액션(서버): `web/src/app/process/[id]/actions.ts`
- 공정화면: `ProcessView.tsx`(카드형, 액션툴바, 조합찾기, 나누기/수정 모달, 토스트), 작성: `EntryGrid.tsx`
- 열/계산: `web/src/lib/types.ts`(io/work/entry 4형, fmtWeight/fmtInt/round2/shipWeight/lossOf/lossRateOf), 일련번호: `serial.ts`, Enter이동: `enterNav.ts`
- 탭/입력/테마: `components/{TabBar,NumberInput,DateToggle,ThemeToggle}.tsx`
- 스키마: `supabase/migrations/0001~0004`, `seed.sql` / **정밀 스펙 기준**: `docs/05_정밀스펙.md`

---

## ★ 2026-05-31 업데이트 (작업 로그)
- **엑셀 원본 정밀 재파악 완료** → `docs/05_정밀스펙.md` (= 컬럼/로직 최종 기준, 01_분석.md 열표 폐기). openpyxl 직접 덤프 + VML 버튼매크로 1045개 매핑.
- **사용자 지침**: ①시트는 엑셀과 100% 동일(열·수식·로직, 비고 뒤 열 포함) ②상단 버튼 전부 구현 ③제거=입출로그탭·재고검증·재고모니터·로스HUD ④품질결산서=LAN 타PC→자기PC 로컬경로 저장.
- **진행 방식 결정**: DB스키마부터 재설계 → 공정화면 재작성 → 버튼 액션 순.
- **완료**: `supabase/migrations/0003_excel_columns.sql`(weight_in·prev_part_name·moved_at·moved_to_name 추가) — **사용자가 적용 필요**. `web/src/lib/types.ts` io/work/entry 4형 열·계산식 엑셀과 1:1 재작성(weight=중량, weight_in=입중량, ship=IF(X=0,O,O+P-W)).
- **완료(2026-05-31 후반)**: actions.ts 전면 재작성(sendRows/completeLots(=집계)/feedToWork(투입)/feedToOtherDept(타부서투입)/relayToWork(이관)/shipToIo(현장·검수출고)/splitLot/updateLot/deleteLots — 05스펙 매핑대로). ProcessView.tsx 모던 대시보드 UI(카드형, 컨텍스트 액션툴바, 대상 드롭다운+실행)로 재작성 + **작업후 입력버그 수정**(중첩 Table 컴포넌트 제거→input remount 해결). EntryGrid Tag검증 제거+모던화. NumberInput onBlurExtra 추가. page.tsx allProcesses 전달. **빌드 green**(Next16 tsc+eslint+build 통과).
- ⚠️ **사용자: `0003_excel_columns.sql` 미적용 시 런타임 insert 오류**(moved_at/weight_in/prev_part_name/moved_to_name 컬럼). 반드시 적용.
- **완료(2026-05-31 추가 라운드)**: ①좌/우 배타선택 ②셀 비율(%)너비→가로스크롤 제거+적응형, 폰트 10px+줄바꿈(… 잘림 방지) ③이전파트 날짜시간(partStamp) ④잠금행 선택+🔓해제·삭제 버튼 ⑤네이티브모달→중앙 토스트/확인 ⑥수정 인라인 패널(✏️) ⑦천단위 콤마(fmtInt/fmtWeight)+round2 부동소수점 방지 ⑧출고시간=일HH:MM·납기=MM-DD ⑨**Tag보정(Module14)·Tag확정(Module36)·장부인쇄(window.print+print:hidden)** 구현. → **공정/검수 툴바 스텁 전부 해소**. 빌드 green.
- **남은 기능**: **일마감만**(매일 마감+미작업 이월, 엑셀과 동일 — **월마감 불필요**), 품질결산서(Module37 로컬경로 저장), 로그인+RLS 잠금. (~~중량오류 Realtime 경고~~ = 사용자 지시로 **제외**). 홈 대시보드 모던화는 단순 UI라 맨 마지막.
- **2026-05-31 추가 요청 구현**: 공정 작업중 선택 행들의 **중량 합 표시 UI** + **목표중량 조합찾기**(작업중 미완료 항목 2개↑ 조합 중 목표에 가장 근사한 5개 → ①~⑤ 버튼 클릭 시 해당 행 자동 체크+합계 표시). 조합 합산 기준 = 작업중 중량(K)=lots.weight.
- **사용자 재확인 대기 로직**: feedToWork의 중량(K)=원본 중량+Tag+Q+원중량 합(VBA D+E+F+H), shipToIo 실중량=작업후−Tag−Q−원중량, completeLots 작업전=중량(K)합. 전부 VBA 그대로 — 사용자가 로직 검증 예정.

---

# (이전) 진행 상태 (저장 시점: 2026-05-29)

다음 세션에서 이어서 작업하기 위한 핸드오프 메모.

## 한 줄 요약
엑셀 `업로드.xlsm`(귀금속 제조공정 중량/로스/입출고 시스템)을 Next.js+Supabase+Vercel(무료)로 이전 중.
골격·DB·작성 입력·탭 네비·다크모드까지는 동작. **공정 화면의 핵심 액션 로직(완료/집계/투입/이관/분할)은 아직 원본과 불일치 — 다음에 하나씩 재검증 필요.**

## 실행/환경
- 앱: `web/` (Next.js 16, App Router). `cd web && npm run dev` → http://localhost:3000
- `web/.env.local` 에 Supabase URL + publishable(anon) key 설정됨(사용자 로컬).
- Supabase 클라우드 무료 프로젝트. SQL 적용 순서:
  1. `supabase/migrations/0001_init.sql`
  2. `supabase/seed.sql`
  3. `supabase/migrations/0002_locked.sql`  ← lots.locked 컬럼 (필수)
  4. 임시 RLS 허용(로그인 전 확인용): `*_anon_all` 정책 (이미 적용됨, 로그인 슬라이스에서 잠글 것)

## 지금까지 만든 것 (동작 확인됨)
- 공정 마스터 40개 시드, 18K/14K·io/work/entry·검수 구분
- **작성**: 다중행 입력 → 대상 공정 일괄 보내기(입고), Tag검증(1tag=0.035g), 일련번호 최초 생성
- **탭 네비**: 18K/14K → 부서/공정/검수 → (공정: 연마/빠우/뻥) 계단식 필터 + 작성/대시보드 고정탭
- **대시보드**: v_process_balance로 파트별 입고/출고/재고
- **다크모드 토글**(밝게/어둡게/시스템), **작업일 토글**, 중량 소수 2자리(셋째자리 입력 차단)

## ★ 미해결: 공정 액션 로직 (다음 세션 최우선)
사용자 피드백 — "로직이 다 잘못되어 있다, 하나씩 잡아야 함".
원본 VBA와 1:1 재대조하며 검증할 대상:
- **완료 처리/집계** (Module7 集計): 작업중→완료 전이. 일련번호 유지/그룹화, 작업전=입중량, 작업후/실중량 수동.
  → 현재 구현(`completeLots`)이 실제 워크플로우(어느 버튼/순서/대상열)와 맞는지 재확인.
- **투입**(Module4, 작업중 A:K→대상) vs **이관**(Module9, 완료 M:Z→대상 B:K) 컬럼 매핑 정확성.
- **출고**(Module10 출고_선택행_복사_공용), **타부서투입**(Module16) 아직 미구현.
- 분할(Module1/11 SplitNumberByCount), 삭제/위로올림(Module6/8/23/24/32) 재확인.
- io 계열(기계/검수)의 입고→출고 전이가 work와 어떻게 다른지(실중량/Tag수정/Tag중량/Tag로스/출고중량) 원본 확인.

### 재검증 방법(권장)
- `_vba/`에 디컴파일된 86개 모듈 원본 있음. 기능별로 한 개씩:
  1. 해당 VBA Sub의 입력(선택 범위)·출력(대상 열)·부수효과(색칠/잠금/로그) 정독
  2. 사용자에게 "이 동작이 맞냐" 확인
  3. 그 다음에 web 액션(`web/src/app/process/[id]/actions.ts`) 수정
- 즉, 코드 먼저 짜지 말고 **VBA 정독 → 사용자 확인 → 구현** 순서.

## 코드 위치
- 액션: `web/src/app/process/[id]/actions.ts` (sendRows/completeLots/transferInbound/transferOutbound/splitLot/updateLot/deleteLots)
- 공정 화면: `web/src/app/process/[id]/ProcessView.tsx`, `page.tsx`, 작성=`EntryGrid.tsx`
- 컬럼정의/계산: `web/src/lib/types.ts` (COLUMNS, shipWeight/lossOf/lossRateOf)
- 일련번호 그룹: `web/src/lib/serial.ts`
- 탭/날짜/테마: `web/src/components/{TabBar,DateToggle,ThemeToggle,NumberInput}.tsx`
- 스키마: `supabase/migrations/0001_init.sql`, `0002_locked.sql`, `supabase/seed.sql`
- 분석/계획: `docs/01_분석.md`, `docs/02_리팩토링계획.md`, `docs/03_셋업가이드.md`

## 확정된 도메인 규칙(불변)
- 일련번호: 최초 1회 생성(작성→보내기, 대상 약어 M_/QM_…), 이동 시 **유지**, 병합/분할 때만 형태 변경.
- 처리·이동된 원본 행은 **잠금**(재작업 불가).
- 작업후/실중량/Tag류는 **수동 입력**, 로스=작업전−작업후·출고중량=실중량+Tag−Tag중량 자동.
- 공정 2계열: io(일반+검수) / work(연마·뻥·빠우).
- 마감=일/월, 미작업 재고 이월. 품질결산서=LAN 공유 .xlsm 로컬 브릿지. 데스크톱 전용. 단일 공용계정.
